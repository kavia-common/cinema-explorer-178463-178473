{
  "name": "movie_frontend",
  "version": "1.0.0",
  "private": true,
  "description": "React frontend for the cinema explorer app",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "CI=true react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
