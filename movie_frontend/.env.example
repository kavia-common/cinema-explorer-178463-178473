# React App Environment Variables (CRA)
# Copy this file to .env.local and fill in the values.

# Supabase (required for "My Movies" section)
REACT_APP_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
REACT_APP_SUPABASE_KEY=YOUR_PUBLIC_ANON_KEY

# TMDB API (used by Trending and Search)
# Provide either a TMDB v4 API Read Access Token (recommended) or a v3 key.
# This app uses Authorization: Bearer <token> AND also appends ?api_key= for compatibility.
REACT_APP_TMDB_API_KEY=YOUR_TMDB_V4_READ_ACCESS_TOKEN_OR_V3_KEY

# Optional: Site URL used for auth redirects (if you enable Supabase Auth flows)
REACT_APP_SITE_URL=http://localhost:3000/
